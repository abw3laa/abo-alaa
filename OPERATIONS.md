# التشغيل والمراقبة (Operations)

هذا الملف يغطي ما **لا** يمكن تنفيذه كجزء من كود التطبيق نفسه (نسخ
احتياطي لقاعدة البيانات، مراقبة توفّر) لأنه إعداد على مستوى منصة
الاستضافة/الخدمة الخارجية، وليس تعديلاً في الكود.

## 1. النسخ الاحتياطي لقاعدة البيانات (Database Backups)

المشروع يستخدم Postgres عبر `DATABASE_URL`. الخطوات تعتمد على أين
تستضيف قاعدة البيانات فعلياً:

**إن كانت مُستضافة على Vercel Postgres / Neon / Supabase / RDS:**
جميعها توفّر نسخاً احتياطية تلقائية مبنية داخلياً (Point-in-Time
Recovery عادة). المطلوب منك فقط:
1. تفعيل PITR (Point-in-Time Recovery) من لوحة تحكم المزوّد إن لم تكن
   مفعَّلة افتراضياً.
2. ضبط مدة الاحتفاظ (Retention) - 7-30 يوماً معقول لمتجر بحجم متوسط.
3. **اختبار استرجاع فعلي مرة كل ربع سنة على الأقل**: استرجع نسخة إلى
   قاعدة بيانات مؤقتة منفصلة وتحقق أن البيانات سليمة. نسخة احتياطية لم
   تُختبر استرجاعها ليست نسخة احتياطية موثوقة.

**إن كانت مُستضافة ذاتياً (self-hosted Postgres):**
```bash
# نسخة يومية مضغوطة (مثال cron job)
pg_dump "$DATABASE_URL" --format=custom --file="backup-$(date +%F).dump"
# رفعها لتخزين خارجي (S3/Backblaze) - لا تُبقِ النسخ على نفس الخادم فقط
```
استخدم أداة جدولة موثوقة (`pg_backup_rotate`, أو خدمة مُدارة) بدل cron
يدوي بلا مراقبة فشل.

## 2. مراقبة التوفّر (Uptime Monitoring)

لا يوجد أي مراقبة توفّر مفعَّلة حالياً في هذا المشروع. يُنصح بخدمة
خارجية بسيطة (مثال: UptimeRobot، Better Stack، Checkly) تراقب:

- `GET /` (الصفحة الرئيسية) — كل 1-5 دقائق.
- `GET /api/health` **إن أُضيف** (لا يوجد حالياً - راجع القسم 3 أدناه).
- `POST /api/webhooks/payment` — تحقق من أن المسار يستجيب (لا تُرسل
  Payload حقيقي، فقط تحقق من أن الخادم لا يُعيد 5xx بشكل مستمر).

اضبط تنبيهاً (Email/Slack/SMS) عند فشل متكرر (مثال: 3 محاولات متتالية)
لتفادي False Positives من انقطاع شبكي عابر.

## 3. نقطة فحص الصحة (Health Check Endpoint)

`src/app/api/health/route.ts` موجودة فعلاً (تتحقق من اتصال قاعدة
البيانات عبر `SELECT 1` وتُعيد 503 عند الفشل) وتتوافق مع
`HEALTHCHECK` في `Dockerfile`. لا حاجة لإجراء إضافي هنا.

** تصحيح: النسخة الأولى من هذا الملف ادّعت خطأً أن هذا المسار غير
موجود دون التحقق الفعلي من الكود أولاً - تم تصحيح ذلك بعد المراجعة. **

## 4. مراقبة الأخطاء (تمت إضافتها في هذا التدقيق)

Sentry مُهيَّأ الآن (`sentry.server.config.ts`, `sentry.edge.config.ts`,
`sentry.client.config.ts`, `src/instrumentation.ts`) لكنه **معطَّل
افتراضياً** حتى تضبط:
- `SENTRY_DSN` (خادم/حافة)
- `NEXT_PUBLIC_SENTRY_DSN` (متصفح)
- اختيارياً `SENTRY_AUTH_TOKEN` + `SENTRY_ORG` + `SENTRY_PROJECT` لرفع
  Source Maps تلقائياً عند البناء (بدونها، البناء ينجح لكن تظهر أخطاء
  Sentry بأسماء ملفات مُصغَّرة (Minified) أقل وضوحاً).

** لم يُختبر هذا التكامل عبر `npm run build` فعلي في بيئة هذا التدقيق -
اختبره محلياً أولاً، وراجع التحذير المكافئ في SECURITY-HARDENING-REPORT.txt. **
